import com.batmanatorul.api.JavaDatapack;
import com.batmanatorul.api.annotation.Datapack;
//The name variable will be deleted in the next version
@Datapack(name = "TestPack", id = "tp", description = "description")
public class Main extends JavaDatapack {
    @Override
    public void onEnable() {

    }
}
